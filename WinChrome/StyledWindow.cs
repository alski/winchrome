namespace WinChrome
{
    using System.Windows;

    /// <summary>The styled window.</summary>
    public class StyledWindow : Window
    {
        /// <summary>Initialises a new instance of the <see cref="StyledWindow"/> class.</summary>
        public StyledWindow()
        {
            this.InitializeStyledWindow();
        }
    }
}
